interface IConfig {
    category: string;
    items:   {
        disabled: boolean;
        note: string;
        title: string;
        thumb: string;
        extraText: string;
    }[];
}

export const SelectDrawerConfig = [{

}]
